{
    "devDependencies": {
        "@types/bun": "latest",
        "@types/js-cookie": "^3.0.6"
    },
    "peerDependencies": {
        "typescript": "^5"
    },
    "name": "racing-ai",
    "private": true,
    "dependencies": {
        "js-cookie": "^3.0.8"
    },
    "scripts": {
        "build": "bun build index.html --outdir ./out --target browser --minify --sourcemap=linked"
    }
}